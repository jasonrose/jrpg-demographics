mixin ages(metric)
  figure.metric.ages
    each category in metric
      .entry
        each sex in category.keys
          i(class=sex)
        span.age_value= category.percent + '%'
        span.hidden=' ' + category.friendlyAge
